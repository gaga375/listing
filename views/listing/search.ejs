<% layout("/layout/boilerplat.ejs") %>
<body>
    <% if (newsearch.length > 0) { %>
      <h2>All Listingd  From <%=  search1 %></h2>
      <!-- Code to be executed if the condition is true -->
      <div class="row row-col-lg-3 row-col-md-2 row-col-sm-1">
        <% for( let list of newsearch ) { %>
          <div class="card mb-40" style="width: 20rem;">
            <a href="/listings/<%= list.id %>" class="main-a"> <img src="<%= list.image.url %>" class="card-img-top" alt=" i love you t"></a>
            <% 
             let tax = list.price * 0.18;
       let newdata = list.price+tax;
            %>
    
            <div class="card-body">
                <a href="/listings<%= list.id %>" class="main-a">  <p class="card-text"><b> <%= list.title %> </b><br> 
                  <div class="tax">
                  &#8377;  <%=list.price.toLocaleString("en-IN")%>/ Night <i class="gst">+ 18% GST <%=  newdata.toLocaleString("en-IN") %> </i></div><br> </a>  
              </p>
            </div>
          </div>
          <% } %>
              <br><hr>
       </div>
    <% } else { %>
      <!-- Code to be executed if the condition is false -->
       <h1> no data found</h1>
  
    <% } %>

    

</body>