<% layout("/layout/boilerplat.ejs") %>
<!-- Navbar -->

<!-- Main Content -->
 
<div class="w-full px-4">
  <div class="h-18 w-full pt-3 text-3xl ">All Listings</div>
  <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 xl:grid-cols-5 2xl:grid-cols-6 gap-4">
    <% for (let list of alldata) { %>
     <div class="mb-10 ml-20w-40">
   <a href="/listings/<%= list.id %>"> <img class="w-full h-48 object-cover rounded-2xl" src="<%= list.image.url %>" alt=""></a> 
        <div class="mt-2">
          <a href="/listings<%= list.id %>" class="main-a"> <span class="font-semibold block"><%= list.title %></span>
          <span class="text-gray-600">₹<%= list.price %></span></a>
        </div>
      </div>
    <% } %>
  </div>
</div>

<!-- Footer -->

